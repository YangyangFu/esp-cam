{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Set Arduino Sketch to Current File",
            "type": "shell",
            "command": "python3",
            "args": [
                "${workspaceFolder}/.vscode/set_sketch.py",
                "${workspaceFolder}",
                "${relativeFile}"
            ],
            "problemMatcher": [],
            "presentation": {
                "reveal": "silent",
                "panel": "shared"
            }
        }
    ]
}
